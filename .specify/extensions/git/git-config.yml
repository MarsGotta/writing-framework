# Git Branching Workflow Extension Configuration
# Copied to .specify/extensions/git/git-config.yml on install

# Branch numbering strategy: "sequential" (001, 002, ...) or "timestamp" (YYYYMMDD-HHMMSS)
branch_numbering: sequential

# Commit message used by `git commit` during repository initialization
init_commit_message: "[Spec Kit] Initial commit"

# Auto-commit before/after core commands.
# Set "default" to enable for all commands, then override per-command.
# Each key can be true/false. Message is customizable per-command.
#
# AVISO (auto-commit.sh, 2026-07-10): al activarse, el script hace `git add .`
# (barre todo lo no ignorado) y `git commit -m` con un mensaje de UNA línea, sin
# cuerpo ni trailer `Co-Authored-By`. Rompe la convención de mensajes del repo
# (`plan(006): … — detalle` + trailer), así que los commits automáticos son
# puntos de guardado, no historia definitiva: reescríbelos con `git commit
# --amend` o `git rebase -i` antes de publicar. `.gitignore` NO cubre `*.pdf`
# ni `.vivarium/`; los smokes trabajan en `mktemp -d`, fuera del repo.
auto_commit:
  default: false
  before_clarify:
    enabled: false
    message: "[Spec Kit] Save progress before clarification"
  before_plan:
    enabled: false
    message: "[Spec Kit] Save progress before planning"
  before_tasks:
    enabled: false
    message: "[Spec Kit] Save progress before task generation"
  before_implement:
    enabled: false
    message: "[Spec Kit] Save progress before implementation"
  before_checklist:
    enabled: false
    message: "[Spec Kit] Save progress before checklist"
  before_analyze:
    enabled: false
    message: "[Spec Kit] Save progress before analysis"
  before_taskstoissues:
    enabled: false
    message: "[Spec Kit] Save progress before issue sync"
  after_constitution:
    enabled: false
    message: "[Spec Kit] Add project constitution"
  after_specify:
    enabled: false
    message: "[Spec Kit] Add specification"
  after_clarify:
    enabled: false
    message: "[Spec Kit] Clarify specification"
  after_plan:
    enabled: false
    message: "[Spec Kit] Add implementation plan"
  after_tasks:
    enabled: true
    message: "tasks: desglose de tareas generado por /speckit-tasks"
  after_implement:
    enabled: true
    message: "feat: implementación de /speckit-implement (pre-revisión; reescribe el mensaje antes de publicar)"
  after_checklist:
    enabled: false
    message: "[Spec Kit] Add checklist"
  after_analyze:
    enabled: false
    message: "[Spec Kit] Add analysis report"
  after_taskstoissues:
    enabled: false
    message: "[Spec Kit] Sync tasks to issues"
