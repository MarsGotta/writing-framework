# Contratos de la spec 001 (puntero histórico)

Las versiones vigentes viven en `writeonmars/contracts/` (única fuente editable
desde 2026-07-04). Las versiones históricas que esta spec congelaba (su
`citation-contract.md` y `pass-output-schema.md` divergían de las vigentes)
quedan en el historial de git de este directorio.
